import React, { useRef, useState, useEffect } from "react";
import {
  Form,
  Button,
  FormGroup,
  FormControl,
  ControlLabel,
} from "react-bootstrap";

const AdminOrder = () => {
  //date time picker
  const [value, setValue] = React.useState(null);

  return <div className="main__content">Danh sach don hang</div>;
};

export default AdminOrder;
